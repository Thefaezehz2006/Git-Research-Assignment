Pull means fetching the latest changes from a remote repository (like origin) and merging them into your current local branch. The git pull command is essentially git fetch (download changes) + git merge. 
It is used to keep your local code up to date with others' changes.
